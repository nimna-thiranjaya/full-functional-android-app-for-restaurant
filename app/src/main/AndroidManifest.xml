<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.grandlegacyresturant">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GrandLegacyResturant">
        <activity android:name=".CheckoutOrderActivity"></activity>
        <activity android:name=".userfeedss" />
        <activity android:name=".ShowActivity" />
        <activity android:name=".CheckOutAddActivity" />
        <activity android:name=".CheckOutActivity" />
        <activity android:name=".FoodDetails">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddFoodActivity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".FogetPasswordActivity" />
        <activity android:name=".profileUpdateActivity" />
        <activity android:name=".profileActivity" />
        <activity android:name=".signupActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>